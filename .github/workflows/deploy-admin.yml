name: Deploy Admin to GitHub Pages

on:
  push:
    branches:
      - main
      - master
      - develop
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.28.0'
          channel: 'stable'

      - name: Get dependencies
        run: flutter pub get

      - name: Build web for root
        run: flutter build web --target lib/admin_main.dart --base-href "/"

      - name: Deploy to root directory
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web
          destination_dir: ./
          cname: false

      - name: Build web for /admin
        run: flutter build web --target lib/admin_main.dart --base-href "/Jfactory/"

      - name: Deploy to /admin directory
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web
          destination_dir: ./admin
          cname: false

