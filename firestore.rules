rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ 개발용: 모든 문서에 대해 완전히 열어둠
    // ⚠️ 주의: 프로덕션 환경에서는 반드시 보안 규칙을 추가하세요!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
